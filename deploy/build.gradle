dependencies {

}

jar {
  manifest {
    attributes(
      'Main-Class': 'de.lukasbreuer.stockalgorithm.Application'
    )
  }
  from {
    configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
  }
}